/**
 * Сценарии, экраны которых уже перенесены в новый клиент. При `VISUAL_TARGET=next`
 * остальные пропускаются; перенесённый экран обязан совпасть с эталоном legacy попиксельно.
 * Ключ — путь заголовков теста через « › », как в отчёте Playwright.
 */
export const PORTED_SCENARIOS = new Set<string>([
  'вход и регистрация › экран ошибки загрузки сессии',
  'вход и регистрация › экран загрузки профиля',
])
