# Рабочий журнал

Здесь остаются только краткие итоги завершённых существенных задач. Это не стенограмма чата и не список мелких действий.

## Формат записи

### YYYY-MM-DD — Название задачи

- **Цель:** что требовалось получить.
- **Сделано:** какие части проекта изменены или проверены.
- **Проверка:** какие команды, тесты или сценарии выполнены.
- **Вывод:** что важно помнить в следующей работе.
- **Следующий шаг:** если он определён.

## Записи

Новые записи добавляются после завершения существенной задачи или по явной команде пользователя сохранить итоги.

### 2026-09-24 — Обработка заявок преподавателей

- **Цель:** перенести `POST /api/admin/teacher-applications`, сохранив внешний контракт approve/reject и согласовав его с исправлением `SEC-003`.
- **Сделано:** в `AdminModule` добавлены body guard, application use case и Prisma repository. Reject атомарно меняет статус и пишет аудит без уведомления; approve атомарно создаёт роль/профиль, аудит и app-уведомление, после чего отправляет Telegram. Для уже активного преподавателя сохранён legacy-ответ `already_teacher` без повторных side effects; деактивированный сохранённый профиль реактивируется без дублирования карточки.
- **Проверка:** legacy — 234 успешных теста и 7 целевых TODO; NestJS — 261 успешный тест. Characterization и e2e покрывают approve/reject, `already_teacher`, реактивацию, Telegram/web-session, nginx-путь, coercion и ошибки `400/401/403/404`. Также прошли lint, backend typecheck, Prisma validate и обе production-сборки.
- **Вывод:** маршрут получил статус `nest-ready`; теперь готовы 40 из 58 маршрутов, 17 остаются на legacy и 1 покрыт characterization-тестами. Схема БД, Prisma migrations и production routing не менялись.
- **Следующий шаг:** пакетом перенести `POST /api/students` и `POST /api/teacher-application`.

### 2026-09-24 — Административное обновление ученика

- **Цель:** перенести `POST /api/admin/update-student` без изменения существующей последовательности бизнес-операций.
- **Сделано:** в `AdminModule` добавлены body guard, use case и Prisma repository для частичного обновления `lessons_count`/`student_track`, полной замены назначений и аудита. Дубликаты преподавателей сворачиваются, пустой массив очищает связи, переход в `barber` снимает назначения, а неактивные teacher-профили отклоняются по правилу `SEC-003`.
- **Проверка:** legacy — 225 успешных тестов и 7 целевых TODO; NestJS — 256 успешных тестов. Characterization и Nest e2e покрывают coercion, частичное/no-op обновление, replacement, barber, Telegram/web-session, nginx-путь и ошибки `400/401/403/404`. Также прошли lint, backend typecheck, Prisma validate и обе production-сборки.
- **Вывод:** маршрут получил статус `nest-ready`; теперь готовы 39 из 58 маршрутов, 18 остаются на legacy и 1 покрыт characterization-тестами. `BUG-003` — частичная запись профиля перед domain-ошибкой — намеренно сохранён и отмечен TODO; схема БД, Prisma migrations и production routing не менялись.
- **Следующий шаг:** перенести `POST /api/admin/teacher-applications`.

### 2026-09-24 — Назначение преподавателя ученику

- **Цель:** пакетом перенести `POST /api/admin/assign-student` и `POST /api/admin/unassign-student` без расхождения административного workflow.
- **Сделано:** в `AdminModule` добавлены общий body guard, application use case и Prisma repository. Создание/удаление связи, аудит и уведомления обеим сторонам объединены в одну транзакцию; Telegram отправляется преподавателю и ученику после коммита. Сохранены идемпотентные повторы и legacy-правило уровня `barber`; назначение деактивированного преподавателя запрещено согласно `SEC-003`.
- **Проверка:** legacy — 216 успешных тестов и 6 целевых TODO; NestJS — 251 успешный тест. Characterization и Nest e2e покрывают assign/unassign, повторные вызовы, completed/barber, app- и Telegram-уведомления, аудит, Telegram/web-session, nginx-путь и ошибки `400/401/403/404`. Также прошли lint, backend typecheck, Prisma validate и обе production-сборки.
- **Вывод:** оба маршрута получили статус `nest-ready`; теперь готовы 38 из 58 маршрутов, 19 остаются на legacy и 1 покрыт characterization-тестами. Схема БД, Prisma migrations и production routing не менялись.
- **Следующий шаг:** перенести `POST /api/admin/update-student`, затем `POST /api/admin/teacher-applications`.

### 2026-09-24 — Управление ролью преподавателя без потери истории

- **Цель:** перенести `POST /api/admin/teachers` и устранить подтверждённый дефект `SEC-003`.
- **Сделано:** в `AdminModule` добавлены body guard, use case и отдельный Prisma repository. Назначение роли создаёт профиль только при отсутствии, а снятие роли атомарно удаляет роль и назначения, сохраняя профиль и `homework_reviews`; аудит и app-уведомление входят в транзакцию, Telegram отправляется после неё. Admin GET, session, teacher cabinet, review и teacher about теперь считают преподавателя активным только при наличии роли.
- **Проверка:** legacy — 207 успешных тестов и 6 целевых TODO; NestJS — 246 успешных тестов. Characterization фиксирует прежнее каскадное удаление; Nest e2e проверяет validation/auth, Telegram/web-session, nginx-путь, уведомления, деактивацию, скрытие доступа и повторное назначение с тем же профилем. Также прошли lint, backend typecheck, Prisma validate и обе production-сборки.
- **Вывод:** маршрут получил статус `nest-ready`; теперь готовы 36 из 58 маршрутов, 21 остаётся на legacy и 1 покрыт characterization-тестами. Схема БД, Prisma migrations и production routing не менялись.
- **Следующий шаг:** перенести `POST /api/admin/assign-student` и затем `POST /api/admin/unassign-student`.

### 2026-09-24 — Административная модерация ученика

- **Цель:** перенести `POST /api/admin/students` как первый write-маршрут административного блока.
- **Сделано:** в `AdminModule` добавлены body guard, отдельный use case и write-repository. Сохранены четыре действия (`approve`, `reject`, `set_studying`, `set_completed`), дедупликация и проверка преподавателей только для approve с непустым массивом, legacy-сообщения и HTTP 200. Prisma-транзакция объединяет назначения, статус, аудит и in-app уведомление; Telegram отправляется общим gateway после коммита.
- **Проверка:** legacy — 199 успешных тестов и 5 целевых TODO; целевой Nest-набор AdminModule — 26 успешных сценариев. Проверены все переходы, assignment semantics, Telegram/web-session, nginx-путь и ошибки `400/401/403/404`.
- **Вывод:** маршрут получил статус `nest-ready`; теперь готовы 35 из 58 маршрутов, 22 остаются на legacy и 1 покрыт characterization-тестами. Схема БД, Prisma migrations и production routing не менялись.
- **Следующий шаг:** согласовать `SEC-003`, затем перенести `POST /api/admin/teachers` без удаления исторических проверок при снятии роли.

### 2026-09-24 — Проверка домашней работы преподавателем

- **Цель:** перенести `POST /api/teacher/review` без потери статусов, прав доступа и побочных эффектов legacy API.
- **Сделано:** в `TeacherCabinetModule` добавлены body guard и отдельный use case. Prisma repository одной транзакцией блокирует повторную проверку, создаёт review, меняет статус ДЗ, пишет аудит и системное сообщение, создаёт in-app уведомление и одноразовый feedback invite для обычных уроков 5/10/15. Telegram-доставка выполняется после коммита общим gateway; сохранены admin bypass, автоматическое создание teacher-профиля администратора и HTTP 200.
- **Проверка:** legacy — 190 успешных тестов и 5 целевых TODO; целевой Nest-набор — 24 успешных сценария. Проверены approve/revision, trim комментария, рейтинг, системные сообщения, аудит, уведомления, дедупликация feedback invite, Telegram gateway, web-session, nginx-путь и ошибки `400/401/403/404/409`.
- **Вывод:** маршрут получил статус `nest-ready`; теперь готовы 34 из 58 маршрутов, 23 остаются на legacy и 1 покрыт characterization-тестами. Схема БД, Prisma migrations и production routing не менялись.
- **Следующий шаг:** перенести `POST /api/homeworks/:id/comments`, переиспользуя текущую матрицу доступа к работе и уведомления.

### 2026-09-24 — Read-only кабинет преподавателя

- **Цель:** пакетом перенести чтение кабинета преподавателя без изменения legacy-контрактов.
- **Сделано:** добавлен `TeacherCabinetModule` с отдельными use cases для dashboard, списка учеников и профиля с работами, общим Prisma repository и общей teacher/admin policy. Сохранены назначение учеников, активные статусы, агрегаты рейтинга, pending-фильтр по умолчанию, `include_reviewed`, проверки, комментарии, вложения, Telegram/VK/web-session и nginx-пути.
- **Проверка:** legacy — 178 успешных тестов и 5 целевых TODO; NestJS — 220 успешных тестов, включая 11 новых сценариев кабинета преподавателя. Проверены преподавательская область, административный доступ, обе формы путей и ошибки `400/401/403/404`.
- **Вывод:** три маршрута получили статус `nest-ready`; теперь готовы 33 из 58 маршрутов, 24 остаются на legacy и 1 покрыт characterization-тестами. Схема БД, Prisma migrations и production routing не менялись.
- **Следующий шаг:** перенести `POST /api/teacher/review` отдельным этапом из-за транзакционной проверки, уведомлений и feedback invite.

### 2026-09-23 — Пакет административных GET

- **Цель:** одним согласованным этапом перенести все оставшиеся административные read-only маршруты.
- **Сделано:** добавлен отдельный `AdminModule` с семью use cases и общим Prisma repository для `teacher-applications`, `feedback`, `teachers`, `students`, карточки ученика, `homeworks` и `audit`. Сохранены legacy-формы ответов, сортировки, cursor-пагинация, file availability, Telegram/VK/web-session и точные ошибки. `BUG-001` исправлен в Nest: статусы `studying` и `completed` больше не смешиваются.
- **Проверка:** legacy — 167 успешных тестов и 5 целевых TODO; NestJS — 209 успешных тестов, включая 16 новых сценариев AdminModule. Проверены насыщенные агрегаты, обе формы путей, пагинация, фильтры, raw audit meta, `400/401/403/404` и исправленный status filter.
- **Вывод:** семь маршрутов получили статус `nest-ready`; теперь готовы 30 из 58 маршрутов, 27 остаются на legacy и 1 покрыт characterization-тестами. Схема БД, Prisma migrations и production routing не менялись.
- **Следующий шаг:** перенести `POST /api/admin/teacher-applications`.

### 2026-09-23 — Перенос обработки изменений профиля

- **Цель:** перенести `POST /api/admin/profile-edits/:id` без изменения наблюдаемого legacy-поведения.
- **Сделано:** в `ProfilesModule` добавлены отдельные body/params guard и use case; Prisma repository атомарно завершает pending-заявку и при approve обновляет student-профиль, а при reject сохраняет комментарий. Reviewer и best-effort аудит сохраняются для обеих веток. Подтверждён и временно сохранён `BUG-002`: ученик не получает уведомление после обработки, а reject-аудит записывается как `profile_edit_rejectd`.
- **Проверка:** legacy — 151 успешный тест и 5 целевых TODO; NestJS — 193 успешных теста. Проверены approve/reject, неизменность профиля при reject, транзакционные поля заявки, Telegram/web-session, nginx-путь, аудит, отсутствие уведомления и ошибки `400/401/403/404`.
- **Вывод:** маршрут имеет статус `nest-ready`; теперь готовы 23 из 58 маршрутов, ещё 33 остаются на legacy и 2 покрыты characterization-тестами. Схема БД, Prisma migrations и production routing не менялись.
- **Следующий шаг:** перенести `GET /api/admin/teacher-applications`.

### 2026-09-23 — Перенос очереди изменений профилей

- **Цель:** перенести `GET /api/admin/profile-edits` с точным контрактом очереди и admin-only доступом.
- **Сделано:** в `ProfilesModule` добавлены отдельный admin controller и use case; общая функция admin-проверки сохраняет legacy-различие между `404` для неизвестного пользователя и `403` для пользователя без роли. Prisma repository выбирает только pending-заявки, связывает их с текущим student-профилем и пользователем и сортирует по `created_at DESC`.
- **Проверка:** legacy — 142 успешных теста и 4 целевых TODO; NestJS — 184 успешных теста. Проверены полный JSON-контракт и сортировка двух заявок, отсутствие rejected-заявки, Telegram/web-session, nginx-путь и ошибки `400/401/403/404`.
- **Вывод:** маршрут имеет статус `nest-ready`; теперь готовы 22 из 58 маршрутов, ещё 34 остаются на legacy и 2 покрыты characterization-тестами. Схема БД, Prisma migrations и production routing не менялись.
- **Следующий шаг:** перенести `POST /api/admin/profile-edits/:id` с транзакционным одобрением/отклонением, аудитом и уведомлением ученика.

### 2026-09-23 — Перенос заявки на изменение профиля ученика

- **Цель:** перенести `POST /api/student/profile-edit` без изменения ролевой логики и побочных уведомлений legacy API.
- **Сделано:** `ProfilesModule` расширен body guard, use case и Prisma-операциями для student `studying/completed`. Замена pending-заявки стала атомарной: предыдущая отклоняется и новая создаётся в одной транзакции, текущий профиль не меняется. Отдельно сохраняются аудит и app-уведомления всем администраторам; общий Telegram gateway повторяет best-effort доставку имени ученика и не влияет на результат запроса при сбое.
- **Проверка:** legacy — 134 успешных теста и 4 целевых TODO; NestJS — 176 успешных тестов. Проверены Telegram/web-session, nginx-путь, `studying/completed/moderation`, повторная заявка, неизменность профиля, точные данные заявки, аудит, app-уведомление, mock Telegram-доставки, validation и ошибки `400/401/403/404`.
- **Вывод:** маршрут имеет статус `nest-ready`; теперь готовы 21 из 58 маршрутов, ещё 35 остаются на legacy и 2 покрыты characterization-тестами. Схема БД, Prisma migrations и production routing не менялись.
- **Следующий шаг:** перенести `GET /api/admin/profile-edits` для чтения очереди pending-заявок.

### 2026-09-23 — Перенос описания профиля преподавателя

- **Цель:** перенести `POST /api/teacher/about`, сохранив контракт и не дублируя профильную архитектуру ученика.
- **Сделано:** student-профильный модуль обобщён до `ProfilesModule` с общими body parser/guard и Prisma repository, но отдельными student/teacher controllers и use cases. Teacher определяется по внутреннему user ID проверенного principal; описание ограничено 1000 символами, обрезается, пустое значение хранится как `NULL`. Сохранены legacy-порядок validation до credential и точный текст ролевой ошибки.
- **Проверка:** legacy — 122 успешных теста и 4 целевых TODO; NestJS — 164 успешных теста. Проверены Telegram/web-session, nginx-путь, trim, очистка, `updated_at`, отсутствие изменений student-профиля, лимит длины и ошибки `400/401/403`; прошли typecheck и полный набор backend e2e-тестов.
- **Вывод:** маршрут имеет статус `nest-ready`; теперь готовы 20 из 58 маршрутов, ещё 36 остаются на legacy и 2 покрыты characterization-тестами. Схема БД, Prisma migrations и production routing не менялись.
- **Следующий шаг:** перенести `POST /api/student/profile-edit` как создание заявки на изменение профиля.

### 2026-09-23 — Перенос описания профиля ученика

- **Цель:** перенести `POST /api/student/about`, сохранив валидацию, identity boundary и точную семантику пустого описания.
- **Сделано:** добавлен профильный модуль с body guard, тонким controller, application use case и Prisma repository. Описание ограничено 1000 символами, обрезается перед записью, пустое значение хранится как `NULL`; обновляется только student-профиль внутреннего user ID проверенного principal. Сохранён legacy-порядок, при котором body validation выполняется до credential-проверки. На следующем этапе модуль обобщён до `ProfilesModule` для student и teacher.
- **Проверка:** legacy — 113 успешных тестов и 4 целевых TODO; NestJS — 154 успешных теста. Проверены Telegram/web-session, nginx-путь, trim, очистка, `updated_at`, изоляция другого ученика, лимит длины и ошибки `400/401/403`; прошли typecheck и целевые e2e-тесты.
- **Вывод:** маршрут имеет статус `nest-ready`; теперь готовы 19 из 58 маршрутов, ещё 37 остаются на legacy и 2 покрыты characterization-тестами. Схема БД, Prisma migrations и production routing не менялись.
- **Следующий шаг:** перенести `POST /api/teacher/about`, переиспользуя профильный модуль и общий identity boundary.

### 2026-09-23 — Перенос загрузки аватара ученика

- **Цель:** перенести `POST /api/student/me/avatar` с multipart-валидацией, нормализацией изображения и безопасной заменой файла.
- **Сделано:** `StudentAvatarsModule` расширен upload use case и отдельной storage-абстракцией; в backend явно добавлен `@fastify/multipart`. Поток пишется во временный файл, Sharp создаёт JPEG 400×400, Prisma обновляется только после успешной обработки. При ошибке новый файл очищается; старый удаляется после записи БД и только внутри `uploads`. HTTP-контракт legacy сохранён, риск потери старого аватара исправлен.
- **Проверка:** legacy — 104 успешных теста и 4 целевых TODO; NestJS — 144 успешных теста. Проверены Telegram/web-session, nginx-путь, отсутствие файла, превышение лимита, повреждённое изображение, роли, JPEG-результат, обновление БД и удаление старого файла; прошли Prisma validation, typecheck, lint и обе production-сборки.
- **Вывод:** маршрут имеет статус `nest-ready`; схема БД, Prisma migrations и production routing не менялись. После установки зависимости остаются четыре ранее зафиксированных high advisory в Prisma CLI.
- **Следующий шаг:** перенести `POST /api/student/about` как простую авторизованную запись профиля.

### 2026-09-23 — Перенос ролевого доступа к аватару ученика

- **Цель:** перенести `GET /api/students/:student_id/avatar`, сохранив доступ владельцу, назначенному преподавателю и администратору.
- **Сделано:** `StudentAvatarsModule` расширен отдельным HTTP-контроллером и общей операцией ролевого чтения. Prisma repository одной выборкой получает avatar ID, владельца и назначения преподавателей; use case переиспользует безопасное открытие файла. Сохранён legacy-порядок: access-check выполняется до avatar-проверки, включая несуществующий student ID.
- **Проверка:** legacy — 92 успешных теста и 4 целевых TODO; NestJS — 132 успешных теста. Проверены три разрешённые роли, запреты постороннему ученику, неназначенному преподавателю и неизвестному user, Telegram/web-session, nginx-путь, private cache, CORP и ошибки `400/401/403/404`; прошли Prisma validation, typecheck, lint и обе production-сборки.
- **Вывод:** маршрут имеет статус `nest-ready`; production routing, схема БД и Prisma migrations не менялись.
- **Следующий шаг:** перенести `POST /api/student/me/avatar` с multipart-валидацией, нормализацией изображения и безопасной заменой файла.

### 2026-09-23 — Перенос собственного аватара ученика

- **Цель:** перенести `GET /api/student/me/avatar`, сохранив credential-проверку, private cache и безопасную выдачу локального файла.
- **Сделано:** добавлен отдельный `StudentAvatarsModule` с тонким controller, application use case и Prisma repository. Student-профиль ищется по внутреннему user ID проверенного principal; файл открывает общий storage adapter. Сохранены legacy-ответы для неизвестного пользователя, отсутствующего student-профиля, пустого avatar ID и отсутствующего файла.
- **Проверка:** legacy — 81 успешный тест и 4 целевых TODO; NestJS — 120 успешных тестов. Проверены Telegram/web-session, nginx-путь, body, JPEG MIME, private cache, CORP и ошибки `400/401/403/404`; прошли Prisma validation, typecheck, lint и обе production-сборки.
- **Вывод:** маршрут имеет статус `nest-ready`; production routing, схема БД и Prisma migrations не менялись.
- **Следующий шаг:** перенести `GET /api/students/:student_id/avatar` с ролевой проверкой владельца, назначенного преподавателя и администратора.

### 2026-09-23 — Перенос авторизованного вложения работы

- **Цель:** перенести `GET /api/homeworks/:homeworkId/attachments/:attachmentId/file`, сохранив проверку принадлежности вложения и ролевую матрицу.
- **Сделано:** `HomeworkFilesModule` расширен attachment-операцией; Prisma repository одной выборкой связывает вложение с работой и получает назначения, use case проверяет владельца/назначенного преподавателя/администратора и переиспользует общий storage adapter, controller — общий streaming. Сохранён legacy-порядок: отсутствующее или чужое для URL вложение возвращает `404` до проверки доступа.
- **Проверка:** legacy — 72 успешных теста и 4 целевых TODO; NestJS — 110 успешных тестов. Проверены разрешённые роли, запреты постороннему и неназначенному преподавателю, принадлежность работе, local body, photo preview, MIME/CORP, web-session, nginx-путь и ошибки `400/401/403/404`.
- **Вывод:** маршрут имеет статус `nest-ready`; production routing, схема БД и Prisma migrations не менялись.
- **Следующий шаг:** перенести `GET /api/student/me/avatar`, переиспользуя общую identity- и storage-логику.

### 2026-09-23 — Перенос файла исправления работы

- **Цель:** перенести `GET /api/homeworks/:homeworkId/revision/file`, не дублируя уже реализованную файловую и ролевую логику.
- **Сделано:** `HomeworkFilesModule` расширен revision-операцией; repository одной выборкой получает основной и revision file ID, use case переиспользует общую проверку владельца/назначенного преподавателя/администратора и открытие storage, controller — общий streaming. Сохранён legacy-порядок: отсутствующий revision file даёт `404` до access-check.
- **Проверка:** legacy — 60 успешных тестов и 4 целевых TODO; NestJS — 98 успешных тестов. Проверены разрешённые роли, запрет постороннему и неизвестному user, local body, preview, MIME/CORP, web-session, nginx-путь и ошибки `400/401/403/404`.
- **Вывод:** маршрут имеет статус `nest-ready`; production routing, схема БД и Prisma migrations не менялись.
- **Следующий шаг:** перенести `GET /api/homeworks/:homeworkId/attachments/:attachmentId/file` с проверкой принадлежности вложения работе.

### 2026-09-23 — Перенос авторизованного основного файла работы

- **Цель:** перенести `GET /api/homeworks/:id/file`, сохранив выдачу local/Telegram-файлов и строгую матрицу доступа.
- **Сделано:** добавлен `HomeworkFilesModule` с controller, use case и Prisma repository. Доступ разрешён владельцу, назначенному преподавателю и администратору по внутреннему user ID; общий storage adapter отвечает за безопасный путь, Telegram proxy и JPEG-preview. Парсер `preview` вынесен в общий модуль и переиспользуется публичными файлами.
- **Проверка:** legacy — 50 успешных тестов и 4 целевых TODO; NestJS — 88 успешных тестов. Проверены три разрешённые роли, два запрета, неизвестный user, local body, MIME/CORP headers, preview, web-session, nginx-путь и ошибки `400/401/403/404`.
- **Вывод:** маршрут имеет статус `nest-ready`; production routing, схема БД и Prisma migrations не менялись.
- **Следующий шаг:** перенести `GET /api/homeworks/:homeworkId/revision/file` с той же матрицей доступа.

### 2026-09-23 — Перенос списка работ ученика

- **Цель:** перенести `GET /api/student/homeworks` без изменения контракта личного кабинета и без доступа к работам другого ученика.
- **Сделано:** добавлен `StudentHomeworksModule` с тонким controller, application use case и Prisma repository. Агрегат сохраняет приоритет `pending`, все проверки и комментарии, `latest_review`, вложения, признаки local/Telegram-файлов и рейтинг только по `approved`-оценкам; student определяется по внутреннему user ID проверенного principal.
- **Проверка:** legacy — 42 успешных теста и 4 целевых TODO; NestJS — 77 успешных тестов. Проверены полный JSON-контракт, сортировка, рейтинг, revision, комментарии, вложения, изоляция учеников, Telegram/web-session, nginx-путь и ошибки `401/403/404`.
- **Вывод:** маршрут имеет статус `nest-ready`; production routing, схема БД и Prisma migrations не менялись.
- **Следующий шаг:** перенести `GET /api/homeworks/:id/file` с общей проверкой владельца, назначения преподавателя и роли администратора.

### 2026-09-23 — Перенос отметки уведомлений

- **Цель:** перенести `POST /api/notifications/read`, сохранив контракт фронта и запрет изменения чужих уведомлений.
- **Сделано:** общий authentication guard расширен поддержкой `telegram_id` в JSON body; добавлены parser команды, отдельный use case и методы Prisma repository для отметки одной или всех непрочитанных записей. Обновление всегда ограничено внутренним user ID из проверенного principal, а чужой или отсутствующий ID остаётся идемпотентным успехом по legacy-контракту.
- **Проверка:** legacy — 35 успешных тестов и 4 целевых TODO; NestJS — 70 успешных тестов. Проверены точечная/массовая отметка, ownership, nginx-путь, `200`, validation, Telegram credential, несовпадение identity и неизвестный пользователь.
- **Вывод:** оба маршрута уведомлений имеют статус `nest-ready`; production routing, схема БД и Prisma migrations не менялись.
- **Следующий шаг:** перенести `GET /api/student/homeworks` как следующий авторизованный read-маршрут.

### 2026-09-23 — Перенос чтения уведомлений

- **Цель:** перенести `GET /api/notifications`, сохранив контракт фронта и изоляцию данных пользователей.
- **Сделано:** добавлен `NotificationsModule` с controller, use case, Prisma repository и отдельным retention-сервисом. Identity берётся из auth principal; ответ сохраняет `payload`, `read_at`, `created_at`, сортировку, лимит `1..80` и общий `unread_count`. Общие query-парсеры вынесены из feature-модулей.
- **Проверка:** legacy — 28 успешных тестов и 4 целевых TODO; NestJS — 63 успешных теста. Проверены payload JSON/невалидный JSON, unread-счётчик, лимит, Telegram/web-session, nginx-путь, неизвестный user, изоляция и retention.
- **Вывод:** `GET /api/notifications` имеет статус `nest-ready`; production routing, схема БД и Prisma migrations не менялись.
- **Следующий шаг:** перенести `POST /api/notifications/read` с проверкой владельца и общим auth principal.

### 2026-09-23 — Перенос авторизованной витрины

- **Цель:** перенести случайную showcase-выборку approved-работ и её файловую ручку.
- **Сделано:** добавлен отдельный `ShowcaseModule` с тонким controller, двумя use cases и Prisma repository. Общий auth guard проверяет Telegram/VK/web-session credential; выборка сохраняет `limit`, `exclude_ids`, `cycled`, дедупликацию по ученику/файлу и фильтр доступности. Файлы идут через общий storage adapter.
- **Проверка:** legacy — 21 успешный тест и 4 целевых TODO; NestJS — 55 успешных тестов. Проверены фото/видео, completed-ученик, циклическая выборка, дубли, credential, nginx-путь и `400/401/403/404`.
- **Вывод:** оба showcase-маршрута имеют статус `nest-ready`; production routing, схема БД и Prisma migrations не менялись.
- **Следующий шаг:** перенести `GET /api/notifications`, опираясь на общую identity-границу.

### 2026-09-23 — Перенос публичных файлов работ

- **Цель:** перенести основной файл и вложения гостевого портфолио, не перенося дефект `SEC-001`.
- **Сделано:** добавлены controller, use case и Prisma repository для двух маршрутов. Доступ ограничен `approved`-работами профилей `studying`; вложение связывается с homework ID из URL. `FileReferenceService` расширен local/Telegram streaming и JPEG-preview без переноса SQL в бизнес-код; backend использует исправленный `sharp 0.35.4`.
- **Проверка:** legacy — 16 успешных тестов и 4 целевых TODO; NestJS — 39 успешных тестов. Проверены поток, MIME/CORP headers, preview, nginx-путь, `400/404`, отказ для pending и чужого вложения; прошли typecheck, lint и обе production-сборки. Audit больше не содержит advisory `sharp`; остаются четыре ранее зафиксированных advisory в Prisma CLI.
- **Вывод:** оба маршрута имеют статус `nest-ready`; production routing, схема БД и Prisma migrations не менялись.
- **Следующий шаг:** перенести авторизованную витрину approved-работ и её файл.

### 2026-09-22 — Перенос публичного аватара

- **Цель:** перенести `GET /api/guest/students/:student_id/avatar`, сохранив ограничения видимости и безопасную работу с локальными файлами.
- **Сделано:** добавлены repository и use case публичного аватара, потоковый Nest controller и общая абстракция `FileReferenceService`. Legacy adapter определяет uploads рядом с SQLite из `DATABASE_URL`, разрешает realpath и не открывает файл за пределами каталога.
- **Проверка:** legacy — 16 успешных тестов и 4 целевых TODO; NestJS — 28 успешных тестов. Проверены JPEG body, `Cache-Control`, `Cross-Origin-Resource-Policy`, прямой и nginx-пути, некорректный ID, непубличный профиль и отсутствующий файл.
- **Вывод:** маршрут имеет статус `nest-ready`; production routing, рабочая БД и Prisma migrations не менялись.
- **Следующий шаг:** перенести публичные основной и дополнительный файлы работ, разрешая только статус `approved`.

### 2026-09-22 — Перенос публичного профиля портфолио

- **Цель:** перенести `GET /api/guest/students/:student_id/portfolio` и закрыть выдачу pending/revision работ в новом backend.
- **Сделано:** добавлены отдельные controller, use case и Prisma repository публичного профиля. Ответ содержит публичный профиль, преподавателей, рейтинг, только `approved` работы, последнюю одобренную проверку и упорядоченные вложения. Проверка локальных и Telegram-файлов вынесена в `StorageModule`; каталог uploads определяется рядом с SQLite из `DATABASE_URL`.
- **Проверка:** NestJS — 22 успешных теста. Проверены полный JSON-контракт, отсутствие pending/revision работ, рейтинг, последняя проверка, локальный и Telegram-файл, вложения, ошибки `400/404` и nginx-путь без `/api`.
- **Вывод:** маршрут имеет статус `nest-ready`; исправление `SEC-001` действует для списка и профиля NestJS, но публичные файловые маршруты legacy пока остаются небезопасными. Production routing, рабочая БД и Prisma migrations не менялись.
- **Следующий шаг:** перенести публичный аватар, затем файловые маршруты только для approved-работ.

### 2026-09-22 — Перенос публичного списка портфолио

- **Цель:** перенести первый публичный GET в NestJS без раскрытия служебных полей и без закрепления дефекта `SEC-001`.
- **Сделано:** добавлены `PublicPortfolioModule`, controller, use case и Prisma repository для `GET /api/guest/portfolio-students`. Repository использует только Prisma query API, выбирает профили `studying` со связанными работами и проверками, а затем явно вычисляет рейтинг, approved-only счётчик и регистронезависимую сортировку. Общая подготовка временной legacy SQLite вынесена в тестовый helper.
- **Проверка:** legacy — 14 успешных тестов и 4 целевых TODO; NestJS — 16 успешных тестов. Проверены точный whitelist ответа, сортировка, исключение непубличных профилей, nginx-путь без `/api` и approved-only `works_count`.
- **Вывод:** маршрут имеет статус `nest-ready`; форма ответа совместима, единственное осознанное отличие — исправленный публичный счётчик работ. Production routing, рабочая БД и Prisma migrations не менялись.
- **Следующий шаг:** перенести `GET /api/guest/students/:student_id/portfolio`, возвращая только approved-работы и вложения.

### 2026-09-22 — Identity/Auth foundation и перенос session

- **Цель:** создать общую авторизацию нового backend и первой полноценной ручкой проверить совместимость фронтенда с legacy и NestJS.
- **Сделано:** добавлены проверка Telegram init data, VK launch params и web-session, `CurrentPrincipal`, authentication/role guards, repository boundary и use case для `GET /api/session`. Ответ агрегирует роли, профили, рейтинг, преподавателей и уведомления в legacy-формате. Prisma-поля платформенных ID переведены на `BigInt` только на уровне клиента; SQLite не менялась.
- **Проверка:** legacy characterization фиксирует полный session-контракт и три способа входа; Nest e2e проверяет те же сценарии, неизвестного пользователя, nginx-вариант пути и ошибки `400/401/403`. Backend-тесты: 13 успешных; legacy: 12 успешных и 4 целевых TODO.
- **Вывод:** `/api/session` имеет статус `nest-ready`, но production routing и рабочая БД не изменялись. Prisma migrations и `db push` не запускались.
- **Следующий шаг:** перенести первый безопасный публичный GET — `/api/guest/portfolio-students` — с исправлением `SEC-001` только там, где маршрут возвращает работы или их файлы.

### 2026-09-22 — Prisma baseline существующей SQLite

- **Цель:** подключить новый NestJS backend к текущей модели данных без изменения рабочей или production-БД.
- **Сделано:** Prisma 7.10 интроспектировала временную SQLite, созданную legacy-миграциями; зафиксированы 19 моделей, PrismaService с SQLite adapter, PersistenceModule и первый `UserIdentityRepository`. Добавлен drift/e2e-тест, документация ограничений и команды generate/validate.
- **Проверка:** `prisma validate`, backend typecheck/build, два backend-теста, десять активных legacy-тестов, frontend lint/build прошли. Тест сверяет все 19 таблиц и читает роли пользователя через repository.
- **Вывод:** Prisma migrations отсутствуют, production routing и БД не менялись. Audit показывает четыре high advisory в дереве dev Prisma CLI; MySQL не используется, автоматическое обновление на Prisma 8 RC или downgrade не выполнялись.
- **Следующий шаг:** реализовать Identity/Auth foundation и перенести `/api/session`, сохранив контракт для сравнения фронтенда.

### 2026-09-22 — Параллельный NestJS-каркас

- **Цель:** подготовить второй backend-процесс без вмешательства в действующий Fastify API и базу.
- **Сделано:** добавлен независимый NestJS 12 backend с Fastify adapter, TypeScript-сборкой, dev/start-командами и совместимым `GET /health`. Legacy characterization-тесты переведены в строгий Telegram auth и расширены матрицей файлового доступа. Описаны переходная топология и Definition of Done маршрута.
- **Проверка:** legacy — 10 успешных тестов и 4 целевых TODO; NestJS — 1 e2e-тест; прошли frontend lint/build, backend typecheck/build. В новом backend-пакете `npm audit` не нашёл уязвимостей.
- **Вывод:** production-маршруты ещё не переключались, Prisma и БД не подключались. Публичная выдача файла pending-работы подтверждена отдельным legacy-тестом как `SEC-001`.
- **Следующий шаг:** создать Prisma baseline для копии существующей SQLite и repository boundary, не выполняя миграции production-БД.

### 2026-09-22 — Baseline для миграции backend

- **Цель:** подготовить проверяемую исходную точку перед переносом Fastify API на NestJS и Prisma.
- **Сделано:** создан реестр всех 58 маршрутов с доступом, зависимостями, риском и статусом; отдельно зафиксированы решения по совместимости и известные дефекты. Добавлен изолированный characterization-контур на `node:test`: он копирует приложение во временный каталог, запускает Fastify на локальном порту и использует отдельную SQLite-БД.
- **Проверка:** число маршрутов в реестре совпадает с регистрациями Fastify — 58; `npm test` — 5 успешных тестов и 3 целевых TODO; `npm run lint` и `npm run build` прошли.
- **Вывод:** production-код и локальная БД не изменены. Baseline подтверждает три legacy-дефекта: публичность неодобренных работ, доступ преподавателя ко всем активным чатам и неточный фильтр статуса учеников.
- **Следующий шаг:** расширить контрактный набор на авторизацию и права файлов, затем создать минимальный NestJS-каркас с `/health`.

### 2026-09-20 — Фотография ученика видна преподавателю, администратору и гостю

- **Цель:** аватар, загруженный учеником, нигде, кроме его собственного экрана, не показывался.
- **Сделано:** в API добавлены `sendStudentAvatar`, проверка доступа `canUserViewStudentProfile`, endpoint'ы `/api/students/:id/avatar` и `/api/guest/students/:id/avatar`; `has_avatar` добавлен в ответы списка и профиля ученика для администратора, преподавателя (включая дашборд «Проверить») и гостя. На фронтенде появились `getStudentAvatarUrl`, `getGuestStudentAvatarUrl` и общий `studentAvatarImg`; фото подставляется в круги списков учеников у преподавателя и администратора, в шапки профиля ученика у преподавателя, администратора и гостя и в карточку витрины. Без аватара оформление не меняется.
- **Проверка:** `npm run lint` и `npm run build`. Матрица доступа на локальном API: преподаватель ученика, администратор и сам ученик — `200`; чужой преподаватель и посторонний ученик — `403`; ученик вне витрины по гостевому адресу — `404`; ученик без аватара — `404`. Заголовки гостевого ответа: `image/jpeg`, `public, max-age=3600`. Визуальная проверка в браузере не выполнялась: в локальной копии базы записи об аватарах есть, а самих файлов в `data/uploads` нет.
- **Вывод:** профиль ученика рисуют четыре независимые функции (`renderStudent`, `renderTeacherStudent`, `renderAdminStudentProfile`, `renderGuestStudent`), и правку внешнего вида профиля нужно вносить во все.
- **Следующий шаг:** проверить экраны с настоящими аватарами и решить, стоит ли оставлять демонстрационное фото постороннего человека как заглушку для учеников без аватара.

### 2026-09-20 — Просмотр фотографий работ

- **Цель:** в гостевой витрине работа показывалась списком кнопок «Открыть фото», сами снимки не отображались; открытие файла в WebView вело к скачиванию.
- **Сделано:** лента снимков вынесена в общие `homeworkPhotoItems` и `renderHomeworkPhotoStrip` и подключена в гостевом экране, где её не было. Добавлен просмотрщик поверх экрана: оригинал целиком, счётчик, листание стрелками, свайпом и клавиатурой, закрытие по Escape, крестику и фону. Кнопки открытия файла остались только для видео и документов. В `ICO` добавлен `forward`; `polishScreen` больше не затирает заданный вручную `aria-label`.
- **Проверка:** `npm run lint` и `npm run build`; гостевые endpoint'ы отдают превью и оригинал (`200`, JPEG). Визуально проверено вручную в локальной копии с боевыми данными.
- **Вывод:** карусель существовала только в `renderHwView`; гостевой экран рендерится отдельной функцией, и правки экрана работы нужно вносить в обе.
- **Следующий шаг:** `bot/dbBackup.js` копирует базу без WAL — бэкап перед деплоем неполный.

### 2026-09-20 — Выкатка накопившихся изменений на production

- **Цель:** развернуть на сервере версию с входом через сайт, комментариями к ДЗ и конфиденциальной обратной связью; прод оставался на коде начала сентября.
- **Сделано:** восстановлена связь сервера с пересозданным репозиторием (deploy key под отдельным алиасом SSH, `reset --hard origin/main`); в `ecosystem.config.cjs` добавлена передача `TG_WEBAPP_AUTH`, `TELEGRAM_BOT_USERNAME`, `VK_APP_ID`, `VK_APP_SECRET`, `MAX_HOMEWORK_UPLOAD_MB`, `CHAT_ENABLED` в процесс API; в серверный `.env` добавлено имя бота. Миграции досоздали пять таблиц и `students.about_me`.
- **Проверка:** путь миграции заранее прогнан локально на копии боевой базы — данные не пострадали, таблиц стало 19. После деплоя проверены окружение процесса через `/proc`, `pm2 status`, health API и вход в Mini App.
- **Вывод:** до этой выкатки API принимал запросы от имени любого `telegram_id` без подписи Telegram — переменная строгого режима не доходила до процесса. Проверять такие вещи нужно по фактическому окружению процесса, а не по содержимому `.env`.
- **Следующий шаг:** `bot/dbBackup.js` копирует файл базы без WAL, поэтому бэкап перед деплоем неполный; заменить копирование на онлайн-бэкап SQLite.

### 2026-09-14 — Единый демонстрационный интерфейс дневника

- **Цель:** собрать и довести до единого стиля страницы ученика, преподавателя, администратора и гостя.
- **Сделано:** добавлен единый демо-вход с четырьмя ролями, светлая/тёмная тема, заполненные демоданные, публичная гостевая витрина, поиск учеников, категории «Ученик / Стажёр / Барбер», карточки работ и адаптация узких экранов. Добавлены правило снятия преподавателей у барбера и конфиденциальная обратная связь ученика.
- **Проверка:** пройдены роли и основные вкладки в локальном Chromium/Edge; проверены ширины 320 и 390 px, API прав отзывов, `npm run lint` и production `npm run build`.
- **Вывод:** перед реальным запуском нужны проверка Telegram/VK на живых аккаунтах и загрузка реальных фотографий; демопортреты и записи предназначены только для локального просмотра.

### 2026-09-11 — Памятка по личному VPN

- **Цель:** сохранить повторяемую инструкцию по управлению личным VPN-сервером и подключению семьи.
- **Сделано:** добавлен `docs/VPN_RUNBOOK.md` с подтверждённой схемой VLESS/TLS, управлением клиентами в 3x-ui, инструкциями для Happ и FLClashX, HWID-ограничением и диагностикой.
- **Проверка:** документ не содержит адресов, паролей, ключей, QR-кодов, ссылок подписок или персональных данных.
- **Вывод:** в следующих VPN-задачах сначала открыть памятку; изменения клиентов предпочтительно делать в панели, а не вручную в базе.

### 2026-09-08 — Кабинет преподавателя: очередь проверки

- **Цель:** дать преподавателю профиль, отдельную очередь новых домашних заданий и список закреплённых учеников.
- **Сделано:** добавлены вкладки «Профиль», «Проверить», «Ученики» и «Уведомления»; очередь показывает только учеников с ДЗ в статусе ожидания проверки и ведёт к их портфолио. В карточке ученика преподаватель видит обучение, средний балл, метро, описание и работы, но не номер телефона.
- **Проверка:** `npm run lint` и `npm run build` прошли успешно.
- **Вывод:** телефон исключён и из интерфейса, и из ответов API преподавателя; он остаётся доступен только в сценариях ученика и администратора.
- **Следующий шаг:** настроить демонстрационного преподавателя в локальном API и пройти вручную сценарий «Проверить → ученик → оценка/комментарий».

### 2026-09-08 — Заполненный предпросмотр преподавателя

- **Цель:** сделать кабинет преподавателя наглядным для обсуждения дизайна.
- **Сделано:** добавлены редактируемое поле «Обо мне» преподавателя и миграция `teachers.about_me`; в локальный тестовый контур добавлены девять вымышленных учеников, поэтому в демонстрации их десять. Для локального преподавателя используется сгенерированный портрет.
- **Проверка:** `npm run lint`, `npm run build` и список локальных назначений подтвердили десять учеников.
- **Вывод:** демонстрационные имена, фото и записи существуют только в локальной БД и режиме предпросмотра; в production используются реальные данные аккаунтов.

### 2026-09-08 — Комментарии внутри домашнего задания

- **Цель:** дать преподавателю возможность оставлять неограниченное число дополнительных комментариев к любой работе закреплённого ученика, а ученику — отвечать в том же контексте.
- **Сделано:** добавлена таблица `homework_comments`, API добавления комментариев с проверкой доступа и уведомлениями, а также лента комментариев и формы отправки в карточке ДЗ для обеих ролей.
- **Проверка:** преподавательский комментарий и ответ ученика сохранены и прочитаны через API; визуально проверены на странице ДЗ. `npm run lint` и `npm run build` прошли успешно.
- **Вывод:** комментарии отделены от оценки/решения по работе, поэтому преподаватель может возвращаться к уже принятому ДЗ и продолжать обратную связь.

### 2026-09-07 — Демонстрационный дизайн страницы ученика

- **Цель:** показать заполненный профиль ученика в светлой премиальной стилистике с сохранением фирменного золотого акцента.
- **Сделано:** обновлена главная страница ученика: обложка профиля, показатели обучения, раздел «Обо мне», карточки работ и преподавателя, переключатель светлой/тёмной темы. Добавлены сгенерированные иллюстративные портреты исключительно для локального режима `?preview=1`; реальные аккаунты используют собственные аватары.
- **Проверка:** `npm run lint` и `npm run build` прошли успешно; страница вручную проверена в локальном предпросмотре.
- **Вывод:** фотографии из демо не являются фотографиями пользователей и не должны подменять их в production.
- **Следующий шаг:** после утверждения ученической страницы проработать интерфейсы преподавателя, администратора и гостя.

### 2026-09-07 — Вход на обычный сайт через Telegram или VK

- **Цель:** дать пользователям доступ к дневнику через браузер, сохранив подтверждение личности в Telegram/VK.
- **Сделано:** добавлены одноразовые запросы входа и веб-сессии с хранением хэшей токенов, API запуска/проверки входа, подтверждение из Telegram deep-link и из подписанного VK Mini App, экран сайта с двумя кнопками входа.
- **Проверка:** `npm run lint` и production `npm run build` прошли успешно.
- **Вывод:** для живого запуска нужны `TELEGRAM_BOT_USERNAME`, строгая Telegram-проверка и корректно настроенный VK-секрет. Локальный runtime API в текущей среде не стартовал: `better-sqlite3` собран под другую версию Node.js.
- **Следующий шаг:** настроить переменные окружения и проверить оба входа с настоящими Telegram/VK-аккаунтами на тестовом домене.

### 2026-09-07 — Локальный предпросмотр интерфейса

- **Цель:** открыть интерфейс дневника в обычном локальном браузере.
- **Сделано:** добавлен development-only параметр `?preview=1` с отдельным тестовым идентификатором; обновлены контекст проекта и журнал решений.
- **Проверка:** тестовый идентификатор успешно получает пустую сессию от локального API; production-путь защищён условием Vite `DEV`.
- **Вывод:** просмотр через браузер не заменяет проверку настоящего входа из Telegram/VK.
- **Следующий шаг:** запустить интерфейс с параметром предпросмотра и пройти гостевой сценарий.
# Проверка оформления — 2026-09-14

Унифицированы размеры элементов, контраст светлой/тёмной тем, золотые кнопки и фокус клавиатуры. Исправлены невидимые стандартные чекбоксы, перекрывающая экран демо-навигация, разрозненные блоки работ. Добавлены миниатюры в просмотр ученика преподавателем/администратором. Проверены основные вкладки четырёх ролей на ширине 320 px в двух темах без ошибок JavaScript и горизонтального переполнения; осмотрены скриншоты профилей и формы ДЗ на 390 px. Линтер и сборка проходят. Это локальная проверка в Chromium/Edge, не проверка реальных Telegram/VK WebView или Safari.
# Конфиденциальная обратная связь — 2026-09-14

Добавлены отдельная форма ученика и раздел «Отзывы» у администратора. Сообщения хранятся в `private_feedback`; преподаватель не имеет endpoint для чтения или уведомления о них. После принятия уроков 5, 10 и 15 создаётся одно приглашение; Telegram-бот отправляет ссылку на форму при HTTPS `WEB_APP_URL`. Проверено локально: отправка учеником успешна, администратор видит запись, запрос преподавателя получает `403`; временная проверочная запись удалена. Линтер и production-сборка проходят.
