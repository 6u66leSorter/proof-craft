# План поэтапной миграции фронтенда

## Цель и критерий готовности

Перевести клиент с монолитного `src/newFrontApp.js` (ручной `innerHTML`, 67 глобальных `window.__ba_*`, общий мутируемый `state`) на React 19 + TypeScript, **не меняя внешний вид**.

«1 в 1» проверяется автоматически: каждый перенесённый экран обязан попиксельно совпасть с эталонными скриншотами, снятыми с legacy-клиента. Отличия допустимы только как явно записанное решение в этом документе.

## Целевой стек

| Задача | Выбор |
| --- | --- |
| UI | React 19 + TypeScript, Vite |
| Серверные данные | TanStack Query |
| UI-состояние | Zustand (стек экранов, вкладки, модалки, черновики) |
| Навигация | собственный стек экранов в store, как `go`/`back` сейчас; hash-роутинг запрещён — Telegram передаёт `initData` в hash |
| Типы API | `@nestjs/swagger` → `openapi-typescript` (после стабилизации NestJS-контрактов) |
| Моки для dev/unit | MSW |
| Стили | текущие CSS без изменений + `clsx`; без UI-китов (VKUI, Telegram UI) и без Tailwind на этапе переноса |
| Платформа | собственный адаптер над Telegram SDK и `@vkontakte/vk-bridge` из npm |
| Тесты | Playwright (визуальная регрессия), Vitest + Testing Library |

## Принцип безопасности

Legacy-клиент (`src/`, `index.html`, корневые `vite.config.js` и `package.json`) **не изменяется** в ходе миграции и остаётся production-клиентом до явного переключения. Новый клиент живёт в отдельном пакете `frontend/` со своими зависимостями, сборкой и dev-портом — по аналогии с `backend/`. Откат на любом этапе — не переключать или переключить обратно, без правок кода.

## Этапы

### 0. Визуальный baseline legacy-клиента — завершён

- `frontend/visual/`: Playwright снимает production-сборку legacy-клиента. Она собирается отдельным конфигом из `frontend/visual/` во временный каталог; корневой `vite.config.js` и `dist/` не затрагиваются.
- Данные отдаёт настоящий legacy API на временной копии `bot/` и отдельной SQLite с детерминированным сидом. Ручные фикстуры не пишутся: ответы всегда совпадают с реальным контрактом.
- Вход — через web-session каждой роли (production-путь, без dev-only `?preview`).
- Мутирующие запросы (`POST`/`PATCH`) перехватываются в браузере, поэтому тесты не меняют БД и не зависят от порядка.
- Внешние скрипты (Telegram SDK, vk-bridge) заглушены, шрифты Google Fonts отдаются локально из `@fontsource`, анимации отключены — снимки воспроизводимы офлайн.
- Даты сида лежат в будущем (2030 год), чтобы retention-очистка legacy API не меняла данные со временем.
- Матрица: экраны ролей × светлая/тёмная тема × 390×844 и 360×800 — 24 сценария, 228 эталонов.
- Сравнение без допусков (`threshold: 0`, `maxDiffPixels: 0`); стабильность проверена 5 повторами (480/480), чувствительность — сдвигом на 1px, сменой оттенка, интервала и радиуса.
- Запуск: `npm --prefix frontend run visual:test`; устройство стенда описано в `frontend/README.md`.

### 1. Каркас нового клиента в `frontend/` — завершён

- React 19 + TypeScript + Vite 8, собственные `package.json`, dev-порт 5174 и `frontend/dist`.
- Стили и статика общие с legacy: `src/index.css` импортируется как есть, `publicDir` указывает на корневой `public/`. CSS-бандл нового клиента побайтно совпадает с legacy.
- Перенесены без изменения поведения: определение платформы (Telegram / VK / web-session), API-клиент и заголовки, `bootstrap` с маршрутизацией по ролям, стек экранов (Zustand, как `go`/`back`), кнопка «Назад» Telegram, выход и повтор. Dev-only режимы `?preview=…` не переносятся.
- Перенесены экраны `loading` и `error`; остальные показывают заглушку.
- `npm --prefix frontend run visual:test:next` сравнивает новый клиент с эталонами legacy; сценарии из `frontend/visual/ported.ts` обязаны совпасть попиксельно, остальные пропускаются. Переснимать эталоны в этом режиме запрещено.

### 2. Перенос экранов по одному — в работе

Перенесены: загрузка, ошибка, гостевая витрина (портфолио, страница ученика, просмотр работы с лентой фото и лайтбоксом), вход на сайт (включая ожидание подтверждения и отмену), выбор роли, регистрация ученика, заявка преподавателя, вход администратора, VK-карточка привязки; кабинет ученика — главная, работы, чат, уведомления, профиль, заявка на изменение профиля, обратная связь.


Порядок: loading/error → гость → вход и регистрация → ученик → ДЗ (новое, просмотр, правка) → преподаватель → администратор → чат.

Для каждого экрана: разметка переносится буквально (классы и inline-стили), данные — через TanStack Query; экран принят, когда его снимки совпадают с baseline legacy и сценарий добавлен в `frontend/visual/ported.ts`.

Правила переноса, выясненные на практике:

- Итоговый DOM legacy — это HTML из `render*()` **после** `polishScreen()`. Он поднимает любой inline `font-size` меньше 12px до 12px, ставит `role="button"` и `tabIndex=0` кликабельным `.card`, проставляет `aria-label`/`title` («Назад», «Выйти», «Открыть чат», «Открыть карточку») кнопкам без текста и `aria-label` полям без подписи, дописывает отдельные блоки (карточка «Ваше мнение важно», подсказки категории ученика, секция работ `ba-works`). Новые компоненты сразу выдают этот итоговый результат.
- CSS минифицируется esbuild (`build.cssMinify: 'esbuild'`): lightningcss по умолчанию в Vite 8 переписывает `rgba()` в hex с округлённой альфой и меняет цвета.
- Иконки — `frontend/src/ui/icons.tsx`, механически сгенерированные из legacy `ICO`; не редактировать вручную.
- Текст с подстановкой пишется одной строкой-шаблоном (`{`🚇 ${metro}`}`), как в legacy. JSX `🚇 {metro}` создаёт два текстовых узла, и Chrome иначе формирует глифы на стыке — сдвиг на 1px.
- Legacy CSS местами выбирает элементы по тексту inline-стиля (`[style*="position:relative"]`), а React сериализует стиль как `position: relative`. Эквиваленты таких правил — в `frontend/src/styles/legacy-compat.css`, привязаны к классу `ba-pos-relative`; его ставят элементам, которые в legacy попадали под эти селекторы.
- Кроме снимков, у экрана есть поведенческие тесты `*.behavior.ts` (навигация, клавиатура, лайтбокс); они проходят на обоих клиентах. Расхождения снимков локализует `node frontend/visual/tools/diff-report.mjs next`.
- Legacy-дефекты переносятся 1 в 1 с комментарием в коде; исправление — отдельное решение с переснятием эталона.
- Перед каждым снимком указатель уводится в угол: иначе элемент под местом последнего клика получает `:hover`, и снимок зависит от раскладки.
- Тела мутирующих запросов сравниваются в поведенческих тестах (`mutations` в фикстуре): оба клиента должны отправлять одинаковый JSON.
- `@vkontakte/vk-bridge` из npm вне VK не отвечает на `send` никогда (legacy вызывал `window.vkBridge` только если скрипт загружен). Вызовы bridge выполняются только при `bridge.isEmbedded()` и с таймаутом.

### 3. Параллельный запуск

- Новый клиент публикуется по отдельному адресу (тестовый путь или тестовый бот/VK-приложение) и работает с тем же API.
- Ручной прогон ролей в Telegram и VK WebView; production по-прежнему обслуживает legacy.

### 4. Переключение и очистка

- Переключение — смена каталога сборки в nginx / URL мини-приложения; откат — обратная смена.
- Legacy `src/` удаляется только после периода стабильной работы и отдельного решения. После этого можно выносить inline-стили в CSS Modules под защитой скриншотов.

## Дефекты legacy, найденные при снятии baseline

Эталоны фиксируют их как есть. При переносе соответствующего экрана нужно решить: повторить 1 в 1 или исправить с переснятием эталона.

- **Гостевая страница ученика** показывает статичные `/demo-homework-*.png` вместо реальных работ и публично выводит работы «На проверке» (связано с `SEC-001`).
- **Чекбоксы назначения преподавателей в светлой теме** выглядят отмеченными, хотя `checked === false`; в тёмной теме отображаются верно.
- **Фото в чате** отображается подписью «Файл» без превью.
- **Свои сообщения в чате** не выравниваются вправо: клиент сравнивает автора с `session.user_id`, которого API не отдаёт.
- **Кнопка чата в карточке преподавателя** на главной ученика подписана для экранных читалок как «Открыть карточку» (`polishScreen` ищет `Chat` с учётом регистра).

## Известные расхождения, требующие решения

- Анимация появления (`.fi`) сейчас проигрывается при каждой полной перерисовке; в React она будет только при монтировании экрана.
- Фокус и прокрутка сейчас сбрасываются при каждом `render()`; в React сохранятся.
- Legacy теряет текст, набранный в поле чата до загрузки ленты (перерисовка заменяет поле); новый клиент текст сохраняет. На снимки не влияет.
- Legacy при каждом переключении вкладки показывает «Загружаем…» до ответа API; новый клиент показывает прошлые данные, пока они обновляются. Итоговый вид совпадает.
- Скриншоты Chromium не заменяют ручной прогон в Telegram и VK WebView (safe-area, BackButton, тема).
